GLOBAL_GROUP_SOURCES(RST SOURCES PATHS
        ${CMAKE_CURRENT_SOURCE_DIR}
        "${PROJECT_SOURCE_DIR}/include/MyDP"
)

PACKAGE_NAME(PACKAGE_NAME MyTemplate ${MyTemplate_VERSION})
ADD_TARGET(MODE "EXE" SOURCES ${SOURCES} TEST ON LIBS "My::${PACKAGE_NAME}_core")